# 이 파일을 복사해 .env 를 만들고 값을 채우세요. (.env 는 git에 올리지 않습니다)
# contracts/contract.md 7절: 프론트/백엔드는 같은 origin으로 호출해야 한다.
# 이 값은 axios의 baseURL이 아니라 vite.config.ts의 dev 서버 프록시 대상(target)으로 쓰인다.
# 즉 npm run dev 중 /api 요청을 이 주소의 백엔드로 전달한다.
# 예) 로컬 백엔드: VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=
